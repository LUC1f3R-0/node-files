const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const stringMake = (value) => (String(value))

module.exports = { add, subtract, multiply, divide, stringMake };
